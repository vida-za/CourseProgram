<UserControl x:Class="CourseProgram.Views.OperationalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CourseProgram.Views"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:beh="clr-namespace:CourseProgram.Behaviors"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Текущие рейсы"
                       Style="{StaticResource Header}" />

            <DataGrid Grid.Row="1"
                      x:Name="dataGridHauls"
                      Margin="0 25 0 0"
                      MaxHeight="150"
                      ItemsSource="{Binding}"
                      SelectedItem="{Binding}"
                      AutoGenerateColumns="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectionChanged">
                        <i:InvokeCommandAction Command="{Binding SelectionChangedCommand}"
                                               CommandParameter="{Binding ElementName=dataGridHauls}" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <i:InvokeCommandAction Command="{Binding}"
                                               CommandParameter="{Binding}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </DataGrid>

            <Grid Grid.Row="2"
                  Margin="10 10 0 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Margin="10 0 0 0"
                         MinWidth="200"
                         IsEnabled="{Binding}"
                         Text="{Binding}" />

                <CheckBox Grid.Column="1"
                          Margin="10 5 0 0"
                          Content="Поиск (по городу)"
                          IsChecked="{Binding}" />
            </Grid>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Текущие заявки"
                       Style="{StaticResource Header}" />

            <DataGrid Grid.Row="1"
                      x:Name="dataGridBids"
                      Margin="0 25 0 0"
                      MaxHeight="150"
                      ItemsSource="{Binding}"
                      SelectedItem="{Binding}"
                      AutoGenerateColumns="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectionChanged">
                        <i:InvokeCommandAction Command="{Binding SelectionChangedCommand}"
                                               CommandParameter="{Binding ElementName=dataGridBids}" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <i:InvokeCommandAction Command="{Binding}"
                                               CommandParameter="{Binding}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </DataGrid>

            <Grid Grid.Row="2"
                  Margin="10 10 0 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Margin="10 0 0 0"
                         MinWidth="200"
                         IsEnabled="{Binding}"
                         Text="{Binding}" />

                <CheckBox Grid.Column="1"
                          Margin="10 5 0 0"
                          Content="Поиск (по городу)"
                          IsChecked="{Binding}" />
            </Grid>
        </Grid>
    </Grid>
</UserControl>