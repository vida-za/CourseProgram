<Page x:Class="CourseProgram.Views.DriverPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DriverPage">

    <Grid>
        <DataGrid x:Name="dgDrivers" AutoGenerateColumns="True" ItemsSource="{Binding Items}">
            <!--<DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="License Number" Binding="{Binding LicenseNumber}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>
            </DataGrid.Columns>-->
        </DataGrid>

        <Button Content="Edit Selected Driver" Click="EditDriver_Click" HorizontalAlignment="Center" Margin="0 10 0 0"/>
    </Grid>
</Page>