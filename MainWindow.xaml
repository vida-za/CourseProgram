<Window x:Class="CourseProgram.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:CourseProgram.Views"
        xmlns:vms="clr-namespace:CourseProgram.ViewModels"
        xmlns:components="clr-namespace:CourseProgram.Components"
        xmlns:custom="clr-namespace:ModalControl;assembly=ModalControl"
        mc:Ignorable="d"
        Closing="Window_Closing"
        Title="MainWindow"
        MinHeight="400"
        MinWidth="800"
        Height="auto"
        Width="auto"
        SizeToContent="WidthAndHeight"
        FontSize="14">

    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type vms:DriverListingViewModel}">
                <views:DriverListingView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:AddDriverViewModel}">
                <views:AddDriverView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:DriverDetailViewModel}">
                <views:DriverDetailView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:MachineListingViewModel}">
                <views:MachineListingView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:AddMachineViewModel}">
                <views:AddDriverView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:MachineDetailViewModel}">
                <views:MachineDetailView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:HomeViewModel}">
                <views:HomeView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type vms:LayoutViewModel}">
                <components:Layout />
            </DataTemplate>
        </Grid.Resources>

        <custom:Modal Panel.ZIndex="1"
                      IsOpen="{Binding IsOpen}">
            <ContentControl Content="{Binding CurrentModalViewModel}" />
        </custom:Modal>

        <ContentControl Content="{Binding CurrentViewModel}" />
    </Grid>
</Window>